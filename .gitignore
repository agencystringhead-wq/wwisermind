node_modules/
.next/
out/
build/
.DS_Store
*.pem
.env*.local
next-env.d.ts
.vercel
*.tsbuildinfo
scratch/

# source images + design reference frames — local build material only.
# Leading slash matters: a bare "images/" would also swallow public/images/,
# which is the folder the site actually ships.
/images/
